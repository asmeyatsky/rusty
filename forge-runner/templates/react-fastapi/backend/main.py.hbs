"""{{app_name}} backend — v{{version}}"""

from fastapi import FastAPI

app = FastAPI(title="{{app_name}}")


@app.get("/")
async def root():
    return {"app": "{{app_name}}", "version": "{{version}}"}


@app.get("/health")
async def health():
    return {"status": "ok"}
